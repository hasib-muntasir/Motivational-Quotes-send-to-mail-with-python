import smtplib
import random
import datetime as dt

MY_MAIL = "ABC@gmail.com" #Use your mail address
MY_PASS = "1234" # Use your password or app password

now = dt.datetime.now().year
print(now)
# with open("quotes.txt", encoding="utf8") as quote_file:
#     all_quotes = quote_file.readlines()
#     quotes = random.choice(all_quotes)
#
# with smtplib.SMTP("smtp.gmail.com", port=587) as connection:
#     connection.starttls()
#     connection.login(user=MY_MAIL,password=MY_PASS)
#     connection.sendmail(
#         from_addr=MY_MAIL,
#         to_addrs = "ABC@gmail.com",
#         msg=f"subject: Motivational Quotes\n\n {quotes}".encode("utf-8")
#     )
